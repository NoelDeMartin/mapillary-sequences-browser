export default class {

    public readonly address: string;
    public readonly country: string;

    constructor(address: string, country: string) {
        this.address = address;
        this.country = country;
    }

}
